# 用户长期记忆表示例

演示时，每位用户使用一个固定的 `user_id`。例如，小王在每次测试时都填写 `demo-xiaowang`，系统就能在新对话中找到他的默认设置。

## 表结构

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 自增编号 |
| user_id | VARCHAR(100) | 测试用户 ID，例如 `demo-xiaowang` |
| memory_key | VARCHAR(50) | 记忆项名称 |
| memory_value | VARCHAR(200) | 记忆项内容 |
| updated_at | TIMESTAMPTZ | 最后修改时间 |

## 演示数据

| id | user_id | memory_key | memory_value |
|---|---|---|---|
| 1 | demo-xiaowang | default_region | 华东 |
| 2 | demo-xiaowang | user_role | 区域负责人 |
| 3 | demo-xiaowang | preferred_name | 小王 |

用户提问时，系统先根据 `user_id` 读取这些记录。比如小王问“2023 年 VONB 是多少”，但没有写地区，系统会使用他的默认地区“华东”继续查询。
